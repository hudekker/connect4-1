const MSG1 = `Player One's Turn (green).  Click on top row to drop chip`;
const MSG2 = `Player Two's Turn (red).  Click on top row to drop chip`;
const MSG3 = `Player One (green) Won!!!`;
const MSG4 = `Player Two (red) Won!!!`;
const MSG_START_OVER = `Play Again!`;

let boolGreen = true;
