const MSG1 = `Player One's Turn (green)\r\nClick on top row to drop chip`;
const MSG2 = `Player Two's Turn (red)\r\nClick on top row to drop chip`;
const MSG3 = `Player One (green) Won!!!\r\nPlay Again?`;
const MSG4 = `Player Two (red) Won!!!\r\nPlay Again?`;
